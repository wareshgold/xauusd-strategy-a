# SP2L F12 Trigger Family Run V1 — 2026-09-09

## Scope

F12 tests the trigger layer independently from entry execution. It encodes only forms directly supported by the currently resolved source evidence and does not promote one form to a universal production classifier.

## Source evidence

The recovered source transcript at approximately 53:16 explicitly describes a one-, two-, or three-candle construction followed by placement of the Limit entry trigger. Earlier source discussion also uses confirmation/bar/key-bar language. Therefore the fixture retains a 1/2/3-candle family and a Key-Bar candidate.

## Fixture result

**PASS — source trigger family preserved without over-specification.**

The fixture represents:

- 1-candle trigger;
- 2-candle trigger;
- 3-candle trigger;
- Key-Bar/confirmation candidate.

All remain `CANDIDATE`; none is silently selected as the canonical universal trigger.

## Execution invariant

A trigger does not imply a market-close entry. The execution model remains a pending Limit candidate. No market-reclaim substitute is introduced.

## What remains unresolved

- exact candle-level acceptance rules;
- whether 1/2/3 candles are alternative trigger families or sequential stages;
- exact role of Key Bar versus trigger;
- required relationship between trigger and relevant Low/High;
- whether every Spike variant permits all trigger forms;
- exact timing rule for placing/canceling the pending Limit.

## Gate decision

**F12 SYNTHETIC TRIGGER FAMILY: PASS**

**SOURCE RESOLUTION: PARTIAL**

**FROZEN GEOMETRY: BLOCKED**

No historical optimization or production trigger classifier is authorized from this fixture.

## Next step

F13: discriminate the 2X concept between the source's half-target language and its separate second-position risk/reward examples, without inventing a universal formula.
