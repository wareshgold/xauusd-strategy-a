# SP2L Entry / SL / Leg2 Triangulation V5 — 2026-09-09

## Scope

Direct inspection of the authoritative source video around 53:16–55:25, cross-checked against the recovered transcript.

## Evidence A — 53:16

Transcript: the teacher describes a one/two/three-candle construction and says the Limit entry trigger can be placed at the demonstrated level. This reinforces that entry is a pending Limit attached to the structural Spike/correction sequence, not a market-close trigger.

## Evidence B — 53:43–54:29

Transcript: the teacher explains a pullback to the start of Leg 2, then describes a renewed `low بالا low بالا` structure. He explicitly discusses a Buy Limit being placed at the demonstrated level and a later 2X outcome.

Decision: `start of Leg 2` is now a high-value candidate for the structural reference used in the correction/entry sequence, but it is NOT frozen as the executable entry price because the source does not yet provide a candle/OHLC formula for that reference.

## Evidence C — 55:02–55:10

Transcript explicitly sequences a clean `low پایین low پایین low پایین` structure followed by `SL اینجا`, `Enter اینجا`, and `leg 2`.

Direct frame inspection shows three separate horizontal regions in the bearish example:

`SL (upper) -> Enter (middle) -> Leg 2 / continuation (lower)`

This is strong visual confirmation that:

- SL is structural invalidation;
- Entry is a separate level inside the corrective setup;
- Leg 2 is a separate continuation/projection objective.

## Evidence D — mirrored structure

The source examples across the inspected interval use the corresponding opposite-side ordering for bullish structures. This supports directional mirroring, but the exact candle-level anchors remain unresolved.

## What is now stronger

1. Entry must remain a pending Limit order.
2. Entry is structurally tied to the Spike/correction sequence.
3. Entry and SL are distinct source-defined levels.
4. SL is not a fixed-distance offset from Entry.
5. Leg 2 is a separate projected continuation objective.
6. The teacher explicitly relates the pullback to the start of Leg 2.

## What remains unresolved

- exact Entry OHLC price;
- whether Entry is the first relevant Low/High, latest higher Low/High, start of Leg 2, or another source-specific level;
- exact SL candle/wick/body anchor;
- exact A/B/C/D anchors;
- AB=CD tolerance;
- exact TP1 geometry;
- deterministic pending-order refresh threshold.

## Gate decision

Do NOT freeze production geometry yet.

The evidence is sufficient to expand the synthetic discrimination matrix with a dedicated candidate `Entry = start-of-Leg2` versus `Entry = relevant prior Low/High` fixture. Historical DEV remains locked until source evidence resolves the candidate or the ambiguity is explicitly represented as unresolved.
