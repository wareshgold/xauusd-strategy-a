# SP2L G281 — Target Bridge Reinspection

Date: 2026-09-12
Status: SOURCE EVIDENCE STRENGTHENED / EXECUTABLE BRIDGE STILL UNRESOLVED

## Scope

Re-inspected the primary SP2L video target-construction sequence around 42:00–44:40 using direct video frames, with emphasis on the transition from the TP1/TP2 schematic to the `Round level` and `250 point` / `500 point` annotations.

## Direct visual observations

- The clean schematic contains `TP2`, `TP1`, `Entry`, and `SL` on one bullish price sequence.
- The next frames explicitly show `Round level 2` and handwritten price examples including approximately `2500`, `3200`, `3250`, and `3255`.
- A subsequent frame explicitly shows `250 point` and `500 point` while the same TP1/TP2/Entry/SL schematic remains visible.

## What is strengthened

1. Point-distance language and target-level construction are part of the same source teaching sequence.
2. Round-level language and target-level construction are part of the same source teaching sequence.
3. The source visually presents TP1 and TP2 as ordered target levels above Entry in the bullish schematic.

## What is NOT established

The inspected frames still do not contain an explicit numeric statement of the form:

- `TP1 = Entry + 250 point`, or
- `TP2 = Entry + 500 point`,

nor an explicit statement defining the point unit, nor an explicit mapping from `Round level 2` to the terminal target.

The frames also do not bridge the schematic to the previously observed order-panel TP values with a unique executable equation.

## Research decision

Keep all of the following as source-confirmed concepts but unresolved executable rules:

- TP1
- TP2
- Round level
- point-distance

Do not infer a target formula from visual spacing alone.

`TARGET_BRIDGE = UNRESOLVED`
`CANONICAL_TARGET_FORMULA = NOT_FROZEN`
`PRODUCTION = BLOCKED`
