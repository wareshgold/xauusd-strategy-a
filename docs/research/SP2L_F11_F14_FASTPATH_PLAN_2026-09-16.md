# SP2L F11–F14 Fast-Path Plan — 2026-09-16

## Purpose

Accelerate source resolution while preserving the source-first gate.

## Parallel tracks

### Track 1 — F11 Refresh

Resolve whether the source gives a deterministic condition for delete/re-place and new sizing. The current evidence establishes behavior but not a numeric threshold. Search primary worked examples for an explicit threshold or unambiguous state transition.

### Track 2 — F12 Trigger

Resolve classifier and precedence across one-, two-, and three-candle structures plus Bar/Key-Bar confirmation. Do not collapse the family to a single candle count unless primary evidence requires it.

### Track 3 — F13 2X

Resolve the exact reference for the half-target example, second-position entry, sizing, stop, and fill semantics. Preserve the concept as optional until a universal executable rule is established.

### Track 4 — F14 AB=CD

Resolve A/B/C/D endpoint definitions, pivot/measurement convention, and tolerance. The current source-confirmed semantic is only that the second leg is expected to match the first leg in magnitude.

### Track 5 — P-Gap

Search primary material for the exact OHLC construction and distinguish P-Gap from E-Gap. A generic three-candle imbalance formula must not be substituted.

## Immediate engineering parallelism

- Keep candidate geometry injectable.
- Keep unresolved fields explicit.
- Add provenance to geometry values.
- Make canonical validation fail closed on unresolved fields.
- Predefine deterministic output/metric schemas.

## Freeze rule

A field becomes canonical only when primary source evidence uniquely determines its executable meaning. Backtest performance is never an admissibility criterion for freezing geometry.

## Next milestone

Produce a consolidated source-resolution update for F11–F14/P-Gap. If any field reaches FULL, update the frozen-geometry matrix immediately; otherwise retain PARTIAL/UNRESOLVED and continue primary evidence acquisition.
