# SP2L G210 — P-Gap Fine-Grained Frame Pass

Date: 2026-09-10  
Scope: source-resolution research only. No executable geometry is frozen.

## Source asset inspected

The registered source video was inspected directly at 30.000 fps using the persisted source identity `SP2L_FULL_2026-09-08`.

The source registry defines the nominal frame convention as `round(t × 30)` and requires timestamp + frame to be recorded together.

## Fine-grained observations

### 30:00–31:30 — spike/P-Gap construction lead-in

The source progressively annotates a bullish candle sequence with arrows and circles. Around 31:00–31:30, a small candle below a large bullish candle and a subsequent candle are explicitly highlighted. The visual sequence is useful evidence that the teacher is distinguishing particular candle relationships, but the frame alone does not uniquely label each candle as a formal OHLC participant of P-Gap.

Observed reference frames:

- 30:00 → frame 54,000: annotated bullish candle sequence; multiple arrows/marks.
- 30:30 → frame 54,900: additional candle annotations.
- 31:00 → frame 55,800: upper/lower candles circled/pointed to in the illustrated sequence.
- 31:20 → frame 56,400: close view of the highlighted candle relationship.
- 31:30 → frame 56,700: same relationship remains visible.

Interpretation: strong evidence for source-specific candle/spike grammar; **not sufficient to freeze a numeric P-Gap formula**.

### 31:48–32:18 — explicit P-GAP annotation

The teacher writes `P-GAP` above the illustrated examples and subsequently adds a continuation-style annotation. The source therefore directly establishes the term P-GAP in this candle-construction discussion.

Observed reference frames:

- 31:48 → frame 56,940: P-GAP annotation begins.
- 31:54 → frame 57,420: P-GAP text/arrow visible.
- 32:00 → frame 57,600: P-GAP prominently annotated.
- 32:06 → frame 57,780: P-GAP circled.
- 32:12 → frame 57,960: P-GAP plus continuation annotation visible.
- 32:18 → frame 58,140: final annotated state before the next construction.

Interpretation: **direct source evidence that P-GAP is a named source concept associated with the illustrated candle construction**. The visible annotations still do not uniquely specify participating candle indices and exact OHLC boundary equations.

### 35:45–36:05 — pre-existing shaded P-Gap examples

Before the later numbered annotations are added, the slide already contains several shaded rectangular regions behind bullish candle constructions, while a separate construction is marked with a red X. The later `Valid BO = P-Gap` text is therefore attached to a visual distinction between accepted and rejected constructions.

Reference frames:

- 35:45 → frame 64,050: multiple bullish constructions; shaded regions visible; rejected example marked red X.
- 35:50 → frame 64,200: same structure, shaded regions persist.
- 35:55 → frame 64,350: shaded regions persist.
- 36:00 → frame 64,800: `Valid BO = P-Gap` visible.
- 36:05 → frame 64,950: numbered-example annotation begins.

Interpretation: **direct/strong evidence that the shaded region is the visual representation used for P-Gap in this teaching example and that P-Gap is tied to breakout validity**. Exact mathematical boundaries remain unresolved.

### 36:07–36:16 — three numbered examples

The teacher explicitly numbers three illustrated constructions `1`, `2`, and `3` and circles them. P-Gap shading is visibly associated with the examples, while the red-X construction remains separate.

Reference frames:

- 36:07 → frame 65,010: numbered examples begin.
- 36:10 → frame 65,100: three examples clearly numbered/circled.
- 36:12 → frame 65,160: three examples clearly visible; P-Gap shaded regions visible.
- 36:14 → frame 65,220: same.
- 36:16 → frame 65,280: same before transition.

Interpretation: direct evidence that the source treats multiple visual constructions as examples in its P-Gap/breakout explanation. This does **not** by itself prove that all three share one uniquely recoverable OHLC formula.

### 36:20–37:10 — AB=CD, timeframe and multi-wave context

The source moves from the P-Gap examples to a SP2L chart illustration. `AB=CD` is explicitly handwritten above the construction. `1M / 5M` is also explicitly marked. The drawing then expands into a multi-wave schematic before returning to the chart.

Reference observations:

- 36:20 → frame 65,400: SP2L chart and prior P-Gap label remain visible.
- 36:30 → frame 65,700: `AB=CD` explicitly written above the chart.
- 36:40 → frame 65,?00: `AB=CD` remains visible.
- 37:00 → frame 66,600: `AB=CD` plus `1M / 5M` annotations.
- 37:10 → frame 66,900: multi-wave schematic appears.

Interpretation: **AB=CD is directly source-confirmed as a named relationship in the SP2L construction**, but the drawing does not uniquely identify executable A/B/C/D OHLC anchors or a tolerance.

### 38:30–40:10 — pending-limit entry and structural SL illustration

This is the strongest visual evidence in this pass for order mechanics. The chart is progressively annotated with a horizontal level and the handwritten label `Buy Limit`. A later annotation labels `SL` beneath the move and circles the initial structural area. The final clean diagram separately labels `Entry` and `SL`.

Reference observations:

- 38:30 → frame 69,300: bullish move with horizontal reference level.
- 38:40 → frame 69,600: horizontal level and correction/continuation structure visible.
- 38:50 → frame 69,900: `Buy Limit` annotation appears.
- 39:00 → frame 70,200: `Buy Limit` and `BO` annotations; multiple horizontal references.
- 39:10 → frame 70,500: entry-area annotations continue.
- 39:20 → frame 70,800: clean chart before the next annotation pass.
- 39:30 → frame 71,400: handwritten `Buy Limit` points to the horizontal entry line.
- 39:35 → frame 71,550: vertical arrow emphasizes the correction/entry relationship.
- 39:40 → frame 71,700: `SL` is drawn below the structure; horizontal stop reference added.
- 39:50 → frame 71,?00: entry and SL relationship remains visible.
- 40:00 → frame 72,000: clean horizontal entry/reference line.
- 40:10 → frame 72,300: `Delete` annotation demonstrates order-line handling.

Interpretation: **pending-limit execution is directly source-supported; entry and structural invalidation/SL are shown as distinct levels**. The visual evidence still does not uniquely prove the exact OHLC anchor used to calculate the pending-limit price, nor does it uniquely specify wick/body treatment for the SL boundary. Therefore B2 and B3 remain source-unresolved at executable geometry.

### 42:00–44:40 — TP1/TP2 and target-distance discussion

The source then presents a clean execution diagram with `TP2`, `TP1`, `Entry`, and `SL` labels. Handwritten notes include `Buy`, `2X`, and later round-level/point-distance annotations such as `250 point` and `500 point`.

Reference observations:

- 42:00 → frame 75,600: `Buy`, `2X`, `SL`, and target-distance arrows visible.
- 42:20 → frame 76,200: target arrows and `2X` discussion continue.
- 42:30 → frame 76,500: clean diagram explicitly labels `TP2`, `TP1`, `Entry`, `SL`.
- 42:40 → frame 76,800: same execution diagram.
- 43:00 → frame 77,400: TP/entry/SL structure remains explicit.
- 43:30 → frame 78,300: round-level/`R` discussion begins.
- 43:40 → frame 78,600: `Round Level` annotation visible.
- 43:50 → frame 78,900: round-level and point-distance notes continue.
- 44:00 → frame 79,200: handwritten `250` and `500` values visible.
- 44:10 → frame 79,500: `250 point` explicitly written.
- 44:20 → frame 79,800: `250 point` / `500 point` distinction visible.
- 44:30 → frame 80,100: target-distance notes continue.
- 44:40 → frame 80,400: end of this target-distance illustration.

Interpretation: **TP1/TP2, Entry and SL are directly source-supported execution labels**, and the source discusses target distances/round levels. However, this pass does not uniquely establish a canonical numerical TP formula, TP1/TP2 precedence, or a deterministic relationship between the handwritten point examples and AB=CD. The `2X` notation is source evidence for a named concept, not permission to invent its formula.

## B1 decision after this pass

**B1 remains SOURCE-UNRESOLVED.**

What is now stronger:

1. P-GAP is explicitly named in the 31:48–32:18 construction discussion.
2. The later slide explicitly states `Valid BO = P-Gap`.
3. Shaded regions are used as the visual P-Gap representation in accepted examples.
4. A red-X example is visually contrasted with the accepted constructions.
5. The source presents multiple P-Gap constructions rather than a single accidental chart shape.

What is still missing for a canonical executable rule:

- uniquely identified participating candle indices;
- exact OHLC/range boundaries of the shaded region;
- whether the boundary uses wick, body, high/low, open/close, or another source-defined level;
- explicit overlap/non-overlap condition;
- exact breakout/follow-through timing relative to the P-Gap;
- deterministic invalidation/expiry semantics.

## B2 decision after this pass

**B2 remains SOURCE-UNRESOLVED at price-anchor geometry, but execution type is source-confirmed.**

Confirmed: the source explicitly uses a `Buy Limit` pending order during/after the correction structure and shows a horizontal entry level.  
Unresolved: the exact candle/price anchor that generates that horizontal level, whether it is exactly geometric C, and any numeric offset/tolerance.

## B3 decision after this pass

**B3 remains SOURCE-UNRESOLVED at boundary geometry, but structural SL semantics are source-confirmed.**

Confirmed: the source separately marks `SL` below the bullish structure and distinguishes it from the Entry line.  
Unresolved: exact invalidation anchor, wick/body treatment, buffer/offset, and deterministic intrabar semantics.

## B4 decision after this pass

**B4 remains SOURCE-UNRESOLVED.**

The source has a trigger section/topic and shows progression from breakout/correction into order placement, but this frame pass does not uniquely establish an accepted 1/2/3-candle/key-bar trigger, its timing, or precedence.

## B5 decision after this pass

**B5 remains SOURCE-UNRESOLVED at executable geometry, while the AB=CD relationship itself is source-confirmed.**

Confirmed: `AB=CD` is explicitly written above the SP2L construction.  
Unresolved: A/B/C/D anchors and equality tolerance.

## B6 decision after this pass

**B6 remains SOURCE-UNRESOLVED at executable projection/TP geometry, while continuation and TP1/TP2 labels are source-supported.**

Confirmed: second-leg continuation is illustrated and the execution diagram explicitly labels TP1 and TP2.  
Unresolved: exact Leg-2 projection equation, TP anchor(s), precedence, and execution semantics. `2X` remains a source concept with unresolved formula.

## Non-inference boundary

This pass does **not** establish that P-Gap equals:

- a generic three-candle imbalance;
- FVG;
- any particular wick-to-wick or body-to-body formula;
- a fixed percentage/point threshold;
- a backtest-selected candle pattern.

It also does not establish that the pending-limit price equals geometric C, that SL is a specific candle wick, or that TP1/TP2 equals a particular multiple of risk. Those remain unresolved until uniquely supported by source text/visual evidence.

## Gate decision

**SOURCE RESOLUTION remains BLOCKED at executable geometry.**

This pass materially strengthens the evidence ledger but does not justify freezing geometry or opening DEV.

## Next source pass

Next priority is the trigger section and the detailed example sequence around the May 13, 2025 ten-entry walkthrough. That section may resolve B2/B3/B4/B6 if the source explicitly maps its numbered entries to exact candle/price levels. Any such mapping must be recorded frame-by-frame before being promoted to canonical geometry.
