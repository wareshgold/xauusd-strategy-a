# SP2L G311 — TP1 / TP2 / Point-Distance Mapping Audit

**Date:** 2026-09-12
**Status:** `TARGET_LADDER_CONFIRMED__NUMERIC_MAPPING_UNRESOLVED`

## Direct frame findings

The source clean schematic explicitly labels TP2, TP1, Entry and SL.

Later frames in the same sequence show:
- `250 point`
- `500 point`
- `1000`

The visual sequence therefore establishes a source-level relationship between target-ladder discussion and point-distance quantities.

## What is not proven

The reviewed frames do not uniquely establish:

`Entry → TP1 = 250 points`

`Entry → TP2 = 500 points`

`Entry → TP2 = 1000 points`

or any other exact assignment.

The handwritten quantities are spatially associated with the schematic, but there is no sufficiently explicit source arrow/equation/label to make the assignment deterministic.

## Decision

`TP1_CONCEPT = CONFIRMED`
`TP2_CONCEPT = CONFIRMED`
`POINT_DISTANCE_CONCEPT = CONFIRMED`
`250_MAPPING = UNRESOLVED`
`500_MAPPING = UNRESOLVED`
`1000_MAPPING = UNRESOLVED`
`POINT_UNIT_CONVERSION = UNRESOLVED`
