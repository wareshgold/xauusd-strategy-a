# SP2L G307 — Source Resolution Next-Evidence Plan

**Date:** 2026-09-12  
**Status:** `SOURCE_ACQUISITION_REQUIRED`

## Highest-value missing evidence

The next source pass should target evidence that can uniquely resolve executable geometry, in this order:

1. A primary-video order example where Entry and SL prices are simultaneously visible and the source construction immediately preceding them is visible.
2. A primary-video example where the measurement tool or labels explicitly identify A/B/C/D.
3. A target-construction example where TP1/TP2 labels and exact price coordinates are simultaneously visible with the 250/500/1000 annotations.
4. Any spoken/text statement explicitly connecting Point Distance or Round Level to TP selection.
5. Any source example that shows how a pending limit is positioned before price returns to it.
6. Any source evidence defining touch/intrabar versus close execution and same-candle ordering.

## Evidence acceptance rule

A candidate interpretation becomes source-confirmed only if the source uniquely identifies the geometry. Visual proximity alone is insufficient.

## Synthetic-fixture transition

Synthetic fixtures may be prepared for competing interpretations, but they cannot be used to choose among them. The fixture phase begins only after the source evidence set is sufficiently explicit to enumerate the candidate geometries without inventing missing parameters.

## Gate

`FROZEN_GEOMETRY = BLOCKED`

`SYNTHETIC_FIXTURES = PREPARATION_ONLY`

`CANONICAL_IMPLEMENTATION = BLOCKED`

`BACKTEST_OPTIMIZATION = BLOCKED`
