# SP2L G48 — CI Fix

Date: 2026-09-09
Gate: G48

## Failure

The complete research-engine suite reached 33 passing tests and one failure in `test_acquisition_blocks_bad_ohlc`.

The fixture intentionally supplied an invalid OHLC row. `parse_twelve_data_time_series()` constructed the canonical `Candle`, whose invariant validation correctly raised `ValueError("OHLC invariant violated")`. The acquisition test expected the provider adapter boundary to normalize malformed provider rows to `ValueError("invalid provider OHLC row")`.

## Fix

The provider adapter now constructs each `Candle` inside the existing provider-row validation boundary and normalizes any `KeyError`, `TypeError`, or `ValueError` from timestamp/OHLC parsing or canonical candle validation into `ValueError("invalid provider OHLC row")`.

This preserves the strategy-neutral boundary: malformed provider data is rejected before any research or Strategy A logic can consume it.

## Gate status

CI rerun required. G48 remains pending until the complete research-engine suite is green and a real local Twelve Data acquisition produces a quality-audit PASS.
