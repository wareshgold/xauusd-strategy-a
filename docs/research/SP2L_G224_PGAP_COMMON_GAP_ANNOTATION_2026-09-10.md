# SP2L G224 — Raw P-Gap / Common Gap Annotation

Date: 2026-09-10
Status: SOURCE VISUAL EVIDENCE — INTERPRETATION CONSTRAINED
Gate: SOURCE RESOLUTION

## Key frame

Raw SP2L source around 32:10–32:25 contains a particularly important handwritten annotation.

At approximately **32:10 (frame 57,300)** the source shows:

- `P-GAP` written prominently above the first spike example;
- an arrow from `P-GAP` toward the illustrated setup;
- on the right, the words `GAP` and `Common` are written separately/underlined.

The annotation is visible continuously across nearby frames, including approximately 31:55, 32:00, 32:05, 32:10, 32:15 and 32:25.

## Why this matters

This visual sequence is important because it weakens the assumption that `P-GAP` can automatically be expanded to **Pressure Gap** merely because a same-author Gap course contains a category called Pressure Gap.

The raw SP2L source itself appears to contrast or relate `P-GAP` with a `Common Gap` concept in the same handwritten teaching sequence.

However, the exact grammatical relationship is not fully recoverable from the handwriting alone. We therefore record the visual evidence without asserting a sentence such as `P-GAP != Common Gap` or `P-GAP = Pressure Gap`.

## Source-safe interpretation

The strongest defensible statement is:

> The raw SP2L source explicitly uses `P-GAP` as a named concept and, in the same teaching sequence, visibly writes `GAP` / `Common` as a separate annotation.

This is stronger evidence that P-Gap is a deliberate source term and may represent a specific Gap subclass or construction rather than a generic reference to any price gap.

## Relationship to the comprehensive Gap course

The comprehensive Gap course lists four categories, including Pressure Gap and Ordinary/Common-type gap concepts. This makes the taxonomy useful, but it still does not establish which category `P-GAP` maps to.

Current competing hypotheses remain:

- H1: P-Gap = Pressure Gap;
- H2: P-Gap = Breakout Gap;
- H3: P-Gap = distinct SP2L-specific Gap construction/subclass;
- H4: P-Gap is a source abbreviation whose expansion is not recoverable from the available visual evidence.

The new `GAP / Common` annotation increases the importance of H3/H4 and means H1 must not be promoted merely from the initial letter `P`.

## Geometry status

This frame still does not uniquely specify:

- participating candle indices;
- exact high/low boundaries;
- wick/body semantics;
- overlap rule;
- minimum gap size;
- whether the shaded region is the exact gap or explanatory highlighting;
- bullish/bearish mirrored formula.

Therefore B1 remains executablely unresolved.

## Decision

No P-Gap formula is frozen from this frame.

No generic FVG implementation is authorized.

No Pressure-Gap rule from the comprehensive course is transferred into Strategy A.

## Next action

Inspect the frames immediately before the `P-GAP` handwriting appears and immediately after it disappears, looking for a spoken/visual transition that names the distinction between P-Gap and Common Gap. If the raw video has a continuous explanatory sequence, it may resolve the acronym/category identity without requiring geometric inference.
