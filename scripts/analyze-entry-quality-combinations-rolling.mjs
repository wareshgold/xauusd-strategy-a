import { readFile, mkdir, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';
import { detectBreakout } from '../src/domain/market/BreakoutDetector.js';
import { detectFollowThrough } from '../src/domain/market/FollowThroughDetector.js';
import { detectSpikeCandidates } from '../src/domain/strategy-a/SpikeDetector.js';
import { detectFirstCorrection } from '../src/domain/strategy-a/CorrectionDetector.js';
import { detectEntryTrigger } from '../src/domain/strategy-a/EntryTrigger.js';
import { getInvalidationRule } from '../src/domain/strategy-a/Invalidation.js';
import { projectLeg2 } from '../src/domain/strategy-a/LegProjection.js';
import { buildEMAContext, buildLocationContext, buildSessionContext } from '../src/domain/strategy-a/Context.js';
import { scoreSetup } from '../src/domain/strategy-a/QualityScore.js';

const ROOT=resolve(process.cwd()), BASE_DIR=resolve(ROOT,'data/reports/strategy-a-baseline'), OUT_DIR=resolve(ROOT,'data/reports/strategy-a-entry-quality-combinations-rolling');
const TOTAL=15000, PRE=10000, FRESH=5000, DEV=6000;
const BREAKOUT_LOOKBACK=5, FT_MAX_BARS=2, SPIKE_MAX_CANDLES=8, SPIKE_MIN_DIRECTIONAL_FRACTION=.5, SPIKE_MAX_OVERLAP_FRACTION=.8;
const CONTEXT={emaPeriod:60,roundStep:50,roundDistance:5,tradingSessions:[{name:'LONDON',startMinutes:420,endMinutes:960},{name:'NEW_YORK',startMinutes:780,endMinutes:1320}],avoidWindows:[]};
const WINDOWS=[[0,2000],[2000,4000],[4000,6000],[6000,8000],[8000,10000]];
function key(c){return `${c.entryIndex}|${c.direction}|${Number(c.entry).toPrecision(15)}|${Number(c.stopLoss).toPrecision(15)}|${Number(c.tp1).toPrecision(15)}`;}
function stats(rows){const rs=rows.map(x=>Number(x.r)).filter(Number.isFinite),w=rs.filter(x=>x>0),l=rs.filter(x=>x<0),gp=w.reduce((a,b)=>a+b,0),gl=-l.reduce((a,b)=>a+b,0);return{n:rs.length,wins:w.length,losses:l.length,winRate:rs.length?w.length/rs.length:0,avgR:rs.length?rs.reduce((a,b)=>a+b,0)/rs.length:0,totalR:rs.reduce((a,b)=>a+b,0),PF:gl?gp/gl:null};}
function norm(c,d){const range=c.high-c.low;if(!(range>0))return{body:0,close:0,opp:0};const buy=d==='BUY';return{body:Math.abs((buy?c.close-c.open:c.open-c.close)/range),close:buy?(c.close-c.low)/range:(c.high-c.close)/range,opp:buy?(Math.min(c.open,c.close)-c.low)/range:(c.high-Math.max(c.open,c.close))/range};}
function buildCandidates(candles,index){const v=candles.slice(0,index+1);if(v.length<Math.max(BREAKOUT_LOOKBACK+2,CONTEXT.emaPeriod))return[];const bo=detectBreakout(v,BREAKOUT_LOOKBACK),ft=detectFollowThrough(v,bo,{maxBarsAfterBreakout:FT_MAX_BARS,requireCloseBeyondBrokenLevel:true}),sp=detectSpikeCandidates(v,bo,ft,{maxCandles:SPIKE_MAX_CANDLES,minDirectionalFraction:SPIKE_MIN_DIRECTIONAL_FRACTION,maxOverlapFraction:SPIKE_MAX_OVERLAP_FRACTION}),out=[];for(const spike of sp.candidates){if(spike.endIndex>=index)continue;const correction=detectFirstCorrection(v,spike);if(!correction||correction.correctionExtremeIndex>=index)continue;const trigger=detectEntryTrigger(v,correction);if(!trigger||trigger.index!==index)continue;const projection=projectLeg2(v,correction);if(!projection)continue;const inv=getInvalidationRule(correction),ema=buildEMAContext(v.map(c=>c.close),CONTEXT);if(!ema)continue;const location=buildLocationContext(trigger.entryPrice,CONTEXT),session=buildSessionContext(trigger.timestamp,CONTEXT),quality=scoreSetup(spike,{ema,location,session});if(!quality.tradeAllowed)continue;const risk=Math.abs(trigger.entryPrice-inv.invalidationLevel),reward=Math.abs(projection.tp1-trigger.entryPrice);if(!(risk>0&&reward>0&&(trigger.direction==='BUY'?projection.tp1>trigger.entryPrice:projection.tp1<trigger.entryPrice)))continue;out.push({entryIndex:index,entry:trigger.entryPrice,stopLoss:inv.invalidationLevel,tp1:projection.tp1,direction:trigger.direction,spike,correction,trigger});}return out;}
function features(candles,c){const n=norm(candles[c.trigger.index],c.direction);return{delay:c.trigger.index-c.correction.correctionExtremeIndex,body:n.body,close:n.close,opp:n.opp};}
function predicates(){return{baseline:()=>true,'delay<=1':r=>r.delay<=1,'delay<=1 AND body>=50%':r=>r.delay<=1&&r.body>=.5,'delay<=1 AND close>=75%':r=>r.delay<=1&&r.close>=.75,'delay<=1 AND oppWick<=25%':r=>r.delay<=1&&r.opp<=.25,'delay<=1 AND body>=50% AND close>=75%':r=>r.delay<=1&&r.body>=.5&&r.close>=.75,'delay<=1 AND close>=75% AND oppWick<=25%':r=>r.delay<=1&&r.close>=.75&&r.opp<=.25,'ALL FOUR':r=>r.delay<=1&&r.body>=.5&&r.close>=.75&&r.opp<=.25};}
async function run(tf){const candles=(JSON.parse(await readFile(resolve(ROOT,`data/historical/xauusd-${tf}.json`),'utf8')).candles??[]);const base=JSON.parse(await readFile(resolve(BASE_DIR,`${tf}.json`),'utf8'));const cutoff=new Date(candles[PRE].timestamp);const canonical=new Map((base.trades??[]).filter(t=>t.result!=='AMBIGUOUS'&&Number.isFinite(Number(t.rMultiple))&&new Date(t.entryTime)<cutoff).map(t=>[key(t),t]));const rows=[];for(let i=0;i<PRE;i++){const cs=buildCandidates(candles,i);if(!cs.length)continue;const c=cs[0],t=canonical.get(key(c));if(!t)continue;rows.push({...features(candles,c),index:i,r:Number(t.rMultiple),entryTime:t.entryTime});}const ps=predicates(),results={};for(const [name,p] of Object.entries(ps)){const all=rows.filter(p);results[name]={overall:stats(all),windows:WINDOWS.map(([a,b])=>({range:[a,b],...stats(all.filter(r=>r.index>=a&&r.index<b))}))};}const report={strategy:'Strategy A',mode:'ENTRY_QUALITY_COMBINATIONS_ROLLING',timeframe,scope:{totalCandles:TOTAL,preHoldoutCandles:PRE,freshHoldoutCandles:FRESH,freshHoldoutExcluded:true,windows:WINDOWS},methodology:{purpose:'Robustness check of pre-registered entry-quality combinations across chronological subwindows.',candidateSelection:'canonical first candidate reconstructed from visible candles',outcomeSource:'canonical baseline outcome joined by exact entry/direction/SL/TP1 key',noThresholdOptimization:true,productionUntouched:true,noFreshHoldout:true},counts:{joined:rows.length},results,decision:'Diagnostic robustness only. No production change and no fresh-holdout authorization by itself.'};await mkdir(OUT_DIR,{recursive:true});const out=resolve(OUT_DIR,`${tf}.json`);await writeFile(out,JSON.stringify(report,null,2));console.log(`${tf}: joined=${rows.length}`);for(const [name,x] of Object.entries(results)){console.log(` ${name}: overall n=${x.overall.n} avgR=${x.overall.avgR.toFixed(4)} PF=${x.overall.PF?.toFixed(3)??'n/a'} | ${x.windows.map(w=>`${w.range[0]}-${w.range[1]} n=${w.n} avgR=${w.avgR.toFixed(3)} PF=${w.PF?.toFixed(2)??'n/a'}`).join(' | ')}`);}}
await run('1min');await run('5min');
