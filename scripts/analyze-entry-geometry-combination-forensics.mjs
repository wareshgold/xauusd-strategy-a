import { readFile, mkdir, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';

const ROOT=resolve(process.cwd());
const OUT_DIR=resolve(ROOT,'data/reports/strategy-a-entry-geometry-combination-forensics');
const TIMEFRAMES=['1min','5min'];
const MIN_SPLIT_N=8;
const TOP_N=30;

function summarize(rows){const a=rows.filter(r=>Number.isFinite(r.rMultiple));const gp=a.filter(r=>r.rMultiple>0).reduce((s,r)=>s+r.rMultiple,0);const gl=a.filter(r=>r.rMultiple<0).reduce((s,r)=>s-r.rMultiple,0);return {n:a.length,wins:a.filter(r=>r.rMultiple>0).length,losses:a.filter(r=>r.rMultiple<0).length,PF:gl?gp/gl:null,avgR:a.length?a.reduce((s,r)=>s+r.rMultiple,0)/a.length:0,totalR:a.reduce((s,r)=>s+r.rMultiple,0)};}
function dd(rows){let e=0,p=0,m=0;for(const r of rows){if(!Number.isFinite(r.rMultiple))continue;e+=r.rMultiple;p=Math.max(p,e);m=Math.max(m,p-e);}return m;}
function bucket(x,cuts,labels){if(!Number.isFinite(x))return null;for(let i=0;i<cuts.length;i++)if(x<cuts[i])return labels[i];return labels.at(-1);}
const defs=[
 ['impulseScore',[1,1.25,1.5,2,2.5,Infinity],['LT_1.00','1.00_1.25','1.25_1.50','1.50_2.00','2.00_2.50','GE_2.50']],
 ['retracement',[0.25,0.5,0.75,1,Infinity],['LT_25%','25_50%','50_75%','75_100%','GE_100%']],
 ['entryLocation',[0.25,0.5,0.75,Infinity],['0_25%','25_50%','50_75%','75_100%']],
 ['distanceFromExtreme',[0.25,0.5,0.75,Infinity],['0_25%','25_50%','50_75%','75_100%']],
 ['delayFromImpulse',[3,6,9,13,Infinity],['D0_2','D3_5','D6_8','D9_12','D13_PLUS']],
 ['impulseBodyFraction',[0.4,0.6,0.8,Infinity],['LT_40%','40_60%','60_80%','GE_80%']],
 ['compressionRatio',[0.75,1,1.25,Infinity],['LT_75%','75_100%','100_125%','GE_125%']],
 ['stopToImpulse',[0.25,0.5,0.75,1,Infinity],['LT_25%','25_50%','50_75%','75_100%','GE_100%']],
];
function classify(rows){return rows.map(r=>{const x={...r};for(const [key,cuts,labels] of defs)x[`${key}Bucket`]=bucket(r[key],cuts,labels);return x;});}
function pairStats(rows,a,b){const [ka,,la]=a,[kb,,lb]=b;const out=[];for(const va of la)for(const vb of lb){const selected=rows.filter(r=>r[`${ka}Bucket`]===va&&r[`${kb}Bucket`]===vb);if(selected.length<MIN_SPLIT_N*3)continue;const n=selected.length;const cut1=Math.floor(n/3),cut2=Math.floor(2*n/3);const dev=selected.slice(0,cut1),validation=selected.slice(cut1,cut2),holdout=selected.slice(cut2);const ds=summarize(dev),vs=summarize(validation),hs=summarize(holdout);out.push({featureA:ka,bucketA:va,featureB:kb,bucketB:vb,dev:{...ds,maxDD:dd(dev)},validation:{...vs,maxDD:dd(validation)},holdout:{...hs,maxDD:dd(holdout)},passesValidation:Boolean(ds.n>=MIN_SPLIT_N&&vs.n>=MIN_SPLIT_N&&ds.PF!=null&&ds.PF>=1&&ds.avgR>0&&vs.PF!=null&&vs.PF>=1&&vs.avgR>0)});}return out;}
function rankCandidates(rows){return [...rows].filter(x=>x.passesValidation&&x.holdout.n>=MIN_SPLIT_N).sort((a,b)=>b.validation.PF-a.validation.PF||b.validation.avgR-a.validation.avgR||b.validation.n-a.validation.n);}
async function run(timeframe){const srcPath=resolve(ROOT,`data/reports/strategy-a-entry-geometry-forensics/${timeframe}.json`);const src=JSON.parse(await readFile(srcPath,'utf8'));const raw=src.tradeRows||[];if(!raw.length)throw new Error(`${timeframe}: entry-geometry source has no tradeRows: ${srcPath}`);const missing=defs.map(([key])=>key).filter(key=>raw.every(r=>!Number.isFinite(r[key])));if(missing.length)throw new Error(`${timeframe}: stale/incompatible entry-geometry report; no finite values for ${missing.join(', ')}. Regenerate it first.`);const rows=classify(raw.filter(r=>Number.isFinite(r.rMultiple)).sort((a,b)=>new Date(a.entryTime)-new Date(b.entryTime)));const coverage=Object.fromEntries(defs.map(([key])=>[key,rows.filter(r=>Number.isFinite(r[key])).length]));const pairs=[];for(let i=0;i<defs.length;i++)for(let j=i+1;j<defs.length;j++)pairs.push(...pairStats(rows,defs[i],defs[j]));const eligible=rankCandidates(pairs);const report={strategy:'Strategy A / SP2L',mode:'RESEARCH_ENTRY_GEOMETRY_COMBINATION_FORENSICS_V2',timeframe,scope:'All baseline trades with geometry features; exhaustive pairwise bucket combinations; chronological three-way split; maximum two features; no production rule changes',methodology:{source:'strategy-a-entry-geometry-forensics tradeRows',search:'all unordered pairs across eight pre-declared geometry features and fixed research buckets',complexity:'maximum two features; no continuous threshold search',split:'first third DEV, second third VALIDATION, final third untouched HOLDOUT, chronological by entryTime',selectionGate:`DEV and VALIDATION each n >= ${MIN_SPLIT_N}, PF >= 1, avgR > 0`,holdout:'Evaluated only after a candidate passes DEV+VALIDATION; never used for candidate ranking or selection',warning:'Pairwise search has multiple-comparison risk. HOLDOUT results are the only untouched evidence in this run and must not be used to tune thresholds.'},coverage:{classifiedTrades:rows.length,featureFiniteCounts:coverage,pairTests:pairs.length,candidatesPassingDevValidation:eligible.length},overall:{...summarize(rows),maxDD:dd(rows)},topValidationCandidates:eligible.slice(0,TOP_N),allPairResults:pairs,nextResearchQuestion:'Freeze a small pre-specified set from DEV+VALIDATION, then report HOLDOUT robustness by BUY/SELL/session/day before any rule change.'};await mkdir(OUT_DIR,{recursive:true});const out=resolve(OUT_DIR,`${timeframe}.json`);await writeFile(out,JSON.stringify(report,null,2));console.log(`${timeframe}: trades=${rows.length} pairTests=${pairs.length} devValidationCandidates=${eligible.length}`);for(const c of eligible.slice(0,TOP_N))console.log(`  ${c.featureA}=${c.bucketA} + ${c.featureB}=${c.bucketB}: DEV n=${c.dev.n} PF=${c.dev.PF?.toFixed(4)??'n/a'} avgR=${c.dev.avgR.toFixed(4)} | VAL n=${c.validation.n} PF=${c.validation.PF?.toFixed(4)??'n/a'} avgR=${c.validation.avgR.toFixed(4)} | HOLDOUT n=${c.holdout.n} PF=${c.holdout.PF?.toFixed(4)??'n/a'} avgR=${c.holdout.avgR.toFixed(4)} totalR=${c.holdout.totalR.toFixed(4)}`);console.log(`Report -> ${out}`);}
for(const tf of TIMEFRAMES)await run(tf);
