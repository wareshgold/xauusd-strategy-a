import {readFile,writeFile,mkdir} from 'node:fs/promises';import {resolve} from 'node:path';import {fileURLToPath} from 'node:url';import {detectBreakout} from '../src/domain/market/BreakoutDetector.js';import {detectFollowThrough} from '../src/domain/market/FollowThroughDetector.js';import {detectSpikeCandidates} from '../src/domain/strategy-a/SpikeDetector.js';import {detectFirstCorrection} from '../src/domain/strategy-a/CorrectionDetector.js';import {detectEntryTrigger} from '../src/domain/strategy-a/EntryTrigger.js';
const ROOT=resolve(fileURLToPath(new URL('..',import.meta.url)));const candles=JSON.parse(await readFile(resolve(ROOT,'data/historical/xauusd-5min.json'),'utf8'));const base=JSON.parse(await readFile(resolve(ROOT,'data/reports/strategy-a-baseline/5min.json'),'utf8'));const PRE=10000,DEV=5000,CFG={breakoutLookback:5,followThrough:{maxBarsAfterBreakout:2,requireCloseBeyondBrokenLevel:true},spike:{maxCandles:8,minDirectionalFraction:.5,maxOverlapFraction:.8}};const WINDOWS=[{name:'DEV_1',start:0,end:1999},{name:'DEV_2',start:2000,end:3999},{name:'DEV_3',start:4000,end:5999},{name:'VAL_1',start:6000,end:7999},{name:'VAL_2',start:8000,end:9999}];
const round=x=>Number.isFinite(x)?Number(x.toFixed(6)):null;const pct=x=>Number.isFinite(x)?Number((x*100).toFixed(4)):null;const median=a=>{const v=a.filter(Number.isFinite).sort((x,y)=>x-y);if(!v.length)return null;const m=Math.floor(v.length/2);return v.length%2?v[m]:(v[m-1]+v[m])/2};const stats=a=>{const n=a.length,totalR=a.reduce((s,x)=>s+x.r,0),wins=a.filter(x=>x.r>0),losses=a.filter(x=>x.r<=0),grossWin=wins.reduce((s,x)=>s+x.r,0),grossLoss=-losses.reduce((s,x)=>s+x.r,0);return{n,avgR:n?totalR/n:null,PF:grossLoss?grossWin/grossLoss:null,WR:n?wins.length/n:null,totalR}};const bucket=(a,k)=>Object.fromEntries([...new Set(a.map(x=>x[k]))].sort().map(v=>[v,stats(a.filter(x=>x[k]===v))]));const consecutiveLosses=a=>{let cur=0,max=0;for(const x of a){if(x.r<=0){cur++;max=Math.max(max,cur)}else cur=0}return max};
function replay(c,i){const v=c.slice(0,i+1);if(v.length<60)return null;const bo=detectBreakout(v,CFG.breakoutLookback),ft=detectFollowThrough(v,bo,CFG.followThrough),sp=detectSpikeCandidates(v,bo,ft,CFG.spike);for(const s of sp.candidates){if(s.endIndex>=i)continue;const co=detectFirstCorrection(v,s);if(!co||co.correctionExtremeIndex>=i)continue;const tr=detectEntryTrigger(v,co);if(!tr||tr.index!==i)continue;const b=bo.find(x=>x.index===s.breakoutIndex&&x.direction===s.direction),f=ft.find(x=>x.breakoutIndex===s.breakoutIndex&&x.direction===s.direction);if(b&&f)return{s,co,tr,b,f}}return null}
function path(c,i,e,sl,tp,dir){let maeR=0,mfeR=0,firstHalfAdverse=null,firstHalfFavorable=null,first1R=null,hitSL=null,hitTP=null;const risk=Math.abs(e-sl);for(let j=i+1;j<Math.min(c.length-1,i+500)+1;j++){const x=c[j],adverse=dir==='BUY'?(e-x.low)/risk,fav=dir==='BUY'?(x.high-e)/risk:(e-x.low)/risk;maeR=Math.max(maeR,adverse);mfeR=Math.max(mfeR,fav);if(firstHalfAdverse===null&&adverse>=.5)firstHalfAdverse=j-i;if(firstHalfFavorable===null&&fav>=.5)firstHalfFavorable=j-i;if(first1R===null&&fav>=1)first1R=j-i;if(hitSL===null&&(dir==='BUY'?x.low<=sl:x.high>=sl))hitSL=j-i;if(hitTP===null&&(dir==='BUY'?x.high>=tp:x.low<=tp))hitTP=j-i;if(hitSL!==null&&hitTP!==null)break}return{maeR,mfeR,firstHalfAdverse,firstHalfFavorable,first1R,hitSL,hitTP}}
const raw=base.trades.filter(t=>{const i=Number(t.entryIndex);return Number.isInteger(i)&&i<PRE&&t.result!=='AMBIGUOUS'&&Number.isFinite(Number(t.rMultiple))});let mismatch=0;const rows=[];for(const t of raw){const i=Number(t.entryIndex),x=replay(candles,i);if(!x||x.tr.timestamp!==t.entryTime||x.tr.direction!==t.direction){mismatch++;continue}const p=path(candles,i,Number(t.entryPrice),Number(t.stopLoss),Number(t.takeProfit1),t.direction);const r=Number(t.rMultiple);rows.push({entryIndex:i,entryTime:t.entryTime,direction:t.direction,session:t.entrySession??'UNKNOWN',split:i<DEV?'DEV':'VAL',window:WINDOWS.find(w=>i>=w.start&&i<=w.end)?.name??'UNKNOWN',r,exceptional:r>=5,maeR:p.maeR,mfeR:p.mfeR,firstHalfAdverse:p.firstHalfAdverse,firstHalfFavorable:p.firstHalfFavorable,first1R:p.first1R,hitSL:p.hitSL,hitTP:p.hitTP,outcome:r>0?'WIN':'LOSS})}
const losses=rows.filter(x=>x.r<=0),wins=rows.filter(x=>x.r>0);const buckets={direction:bucket(rows,'direction'),session:bucket(rows,'session'),outcome:bucket(rows,'outcome'),window:bucket(rows,'window'),split:bucket(rows,'split')};const failureModes={losses:stats(losses),wins:stats(wins),lossMAE:{medianR:median(losses.map(x=>x.maeR)),p50:losses.length?losses.filter(x=>x.maeR>=.5).length/losses.length:null,p75:losses.length?losses.filter(x=>x.maeR>=.75).length/losses.length:null,p90:losses.length?losses.filter(x=>x.maeR>=.9).length/losses.length:null},lossMFE:{medianR:median(losses.map(x=>x.mfeR)),reachedHalf:losses.length?losses.filter(x=>x.mfeR>=.5).length/losses.length:null,reached1R:losses.length?losses.filter(x=>x.mfeR>=1).length/losses.length:null,reached2R:losses.length?losses.filter(x=>x.mfeR>=2).length/losses.length:null},winsMAE:{medianR:median(wins.map(x=>x.maeR))},winsMFE:{medianR:median(wins.map(x=>x.mfeR))},pathTiming:{lossReachedHalfAdverse:losses.length?losses.filter(x=>x.firstHalfAdverse!==null).length/losses.length:null,lossReachedHalfFavorable:losses.length?losses.filter(x=>x.firstHalfFavorable!==null).length/losses.length:null,lossReached1R:losses.length?losses.filter(x=>x.first1R!==null).length/losses.length:null,winReached1R:wins.length?wins.filter(x=>x.first1R!==null).length/wins.length:null,winHitSL:wins.length?wins.filter(x=>x.hitSL!==null).length/wins.length:null,winHitTP:wins.length?wins.filter(x=>x.hitTP!==null).length/wins.length:null},consecutiveLosses:consecutiveLosses(rows)};
const windowFailure=Object.fromEntries(WINDOWS.map(w=>{const a=rows.filter(x=>x.window===w.name),l=a.filter(x=>x.r<=0),n=a.filter(x=>x.r>0);return [w.name,{overall:stats(a),losses:stats(l),wins:stats(n),lossMAEmedianR:median(l.map(x=>x.maeR)),lossMFEmedianR:median(l.map(x=>x.mfeR)),lossReachedHalfAdverse:l.length?l.filter(x=>x.firstHalfAdverse!==null).length/l.length:null,lossReachedHalfFavorable:l.length?l.filter(x=>x.firstHalfFavorable!==null).length/l.length:null,lossReached1R:l.length?l.filter(x=>x.first1R!==null).length/l.length:null,maxConsecutiveLosses:consecutiveLosses(a)}]}));
const result={strategy:'Strategy A / SP2L',mode:'PHASE_17_BASELINE_FAILURE_DECOMPOSITION',timeframe:'5min',scope:{rawBaselinePre:raw.length,canonicalReplayed:rows.length,dev:rows.filter(x=>x.split==='DEV').length,val:rows.filter(x=>x.split==='VAL').length,freshHoldoutExcluded:true,productionUntouched:true},integrity:{expectedCanonical:210,rawBaselinePre:raw.length,canonicalReplayed:rows.length,mismatch},methodology:{purpose:'Descriptive decomposition of baseline trade failures and path behavior; identify common mechanical failure modes without optimizing or selecting thresholds.',pathHorizonBars:500,metrics:['MAE in R','MFE in R','time-to-0.5R adverse','time-to-0.5R favorable','time-to-1R favorable','SL/TP path hits','fixed chronological windows','direction/session splits','maximum consecutive losses'],noOptimization:true,noThresholdSearch:true,noNewTradingRules:true,noFreshHoldoutAccess:true},overall:stats(rows),buckets,failureModes,windowFailure,cases:rows};const out=resolve(ROOT,'data/reports/strategy-a-phase17-baseline-failure-decomposition');await mkdir(out,{recursive:true});await writeFile(resolve(out,'5min.json'),JSON.stringify(result,null,2));const fmt=s=>`N=${s.n} avgR=${round(s.avgR)} PF=${round(s.PF)} WR=${pct(s.WR)}% totalR=${round(s.totalR)}`;console.log(`PHASE_17_BASELINE_FAILURE_DECOMPOSITION 5min N=${rows.length} DEV=${rows.filter(x=>x.split==='DEV').length} VAL=${rows.filter(x=>x.split==='VAL').length} FRESH=LOCKED`);console.log(`INTEGRITY expected=210 raw=${raw.length} actual=${rows.length} mismatch=${mismatch}`);console.log(`OVERALL ${fmt(result.overall)}`);console.log(`LOSSES ${fmt(failureModes.losses)} | WINS ${fmt(failureModes.wins)}`);console.log(`MAE losses median=${round(failureModes.lossMAE.medianR)}R >=0.5R=${pct(failureModes.lossMAE.p50)}% >=0.75R=${pct(failureModes.lossMAE.p75)}% >=0.9R=${pct(failureModes.lossMAE.p90)}%`);console.log(`MFE losses median=${round(failureModes.lossMFE.medianR)}R reached0.5R=${pct(failureModes.lossMFE.reachedHalf)}% reached1R=${pct(failureModes.lossMFE.reached1R)}% reached2R=${pct(failureModes.lossMFE.reached2R)}%`);console.log(`PATH losses: halfAdv=${pct(failureModes.pathTiming.lossReachedHalfAdverse)}% halfFav=${pct(failureModes.pathTiming.lossReachedHalfFavorable)}% 1R=${pct(failureModes.pathTiming.lossReached1R)}% | wins: 1R=${pct(failureModes.pathTiming.winReached1R)}%`);console.log(`MAX_CONSECUTIVE_LOSSES=${failureModes.consecutiveLosses}`);console.log('=== WINDOWS ===');for(const[k,v]of Object.entries(windowFailure))console.log(`${k}: ${fmt(v.overall)} lossMAE=${round(v.lossMAEmedianR)}R lossMFE=${round(v.lossMFEmedianR)}R lossHalfAdv=${pct(v.lossReachedHalfAdverse)}% lossHalfFav=${pct(v.lossReachedHalfFavorable)}% loss1R=${pct(v.lossReached1R)}% maxLossStreak=${v.maxConsecutiveLosses}`);console.log(`REPORT=${resolve(out,'5min.json')}`);console.log('STATUS=DESCRIPTIVE_ONLY NO_OPT NO_THRESHOLD_SEARCH NO_NEW_RULE NO_FRESH PRODUCTION_UNCHANGED');