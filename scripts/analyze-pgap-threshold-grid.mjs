import { readFile, mkdir, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';
const ROOT = resolve(process.cwd());
const INPUT = resolve(ROOT, 'data/reports/strategy-a-pgap-candidate-quality');
const OUTPUT = resolve(ROOT, 'data/reports/strategy-a-pgap-threshold-grid');
const BANDS = [{ name: 'LT_0.02', min: -Infinity, max: .02 }, { name: '0.02_0.05', min: .02, max: .05 }, { name: '0.05_0.10', min: .05, max: .10 }, { name: '0.10_0.20', min: .10, max: .20 }, { name: '0.20_0.40', min: .20, max: .40 }, { name: 'GE_0.40', min: .40, max: Infinity }];
function percentile(values,p){const a=values.filter(Number.isFinite).sort((x,y)=>x-y);if(!a.length)return null;const i=(a.length-1)*p,lo=Math.floor(i),hi=Math.ceil(i);return lo===hi?a[lo]:a[lo]+(a[hi]-a[lo])*(i-lo)}
function stats(rows){const rs=rows.map(r=>r.rMultiple).filter(Number.isFinite),w=rs.filter(r=>r>0),l=rs.filter(r=>r<0),gw=w.reduce((s,r)=>s+r,0),gl=Math.abs(l.reduce((s,r)=>s+r,0));return{trades:rs.length,wins:w.length,losses:l.length,winRate:rs.length?w.length/rs.length:0,avgR:rs.length?rs.reduce((s,r)=>s+r,0)/rs.length:0,totalR:rs.reduce((s,r)=>s+r,0),PF:gl?gw/gl:null,medianR:percentile(rs,.5)}}
function maxDrawdown(rows){let equity=0,peak=0,dd=0;for(const row of [...rows].sort((a,b)=>String(a.entryTime).localeCompare(String(b.entryTime)))){equity+=Number.isFinite(row.rMultiple)?row.rMultiple:0;peak=Math.max(peak,equity);dd=Math.max(dd,peak-equity)}return dd}
function bandFor(v){if(!Number.isFinite(v))return'NA';return BANDS.find(b=>v>=b.min&&v<b.max)?.name??'NA'}
function enrich(rows){return rows.map(r=>({...r,ratio:r.pgap?.maxGapToSpike,ratioBand:bandFor(r.pgap?.maxGapToSpike)}))}
function grid(rows){return Object.fromEntries(BANDS.map(b=>{const subset=rows.filter(r=>r.ratioBand===b.name);return[b.name,{...stats(subset),DD:maxDrawdown(subset)}]}))}
function thresholdStudy(rows){const thresholds=[.02,.05,.10,.20,.40];return Object.fromEntries(thresholds.map(threshold=>{const kept=rows.filter(r=>Number.isFinite(r.ratio)&&r.ratio<=threshold);return[`LE_${threshold.toFixed(2)}`,{threshold,kept:kept.length,coverage:rows.length?kept.length/rows.length:0,...stats(kept),DD:maxDrawdown(kept)}]}))}
async function analyze(timeframe){const report=JSON.parse(await readFile(resolve(INPUT,`${timeframe}.json`),'utf8'));const rows=enrich(report.trades??[]);const chronological=[...rows].sort((a,b)=>String(a.entryTime).localeCompare(String(b.entryTime))),oos=chronological.slice(Math.floor(chronological.length/2));const outObj={strategy:'Strategy A / SP2L',timeframe,sourceReport:`data/reports/strategy-a-pgap-candidate-quality/${timeframe}.json`,purpose:'Diagnostic threshold-distribution study for pre-entry P-GAP candidate size relative to spike size. No threshold is promoted to a trading rule.',fullSample:{trades:rows.length,baseline:stats(rows),baselineDD:maxDrawdown(rows),ratioDistribution:{p25:percentile(rows.map(r=>r.ratio),.25),p50:percentile(rows.map(r=>r.ratio),.5),p75:percentile(rows.map(r=>r.ratio),.75),p90:percentile(rows.map(r=>r.ratio),.9)},byRatioBand:grid(rows),cumulativeUpperThresholds:thresholdStudy(rows)},oosSecondHalf:{trades:oos.length,baseline:stats(oos),baselineDD:maxDrawdown(oos),byRatioBand:grid(oos),cumulativeUpperThresholds:thresholdStudy(oos)},byDirection:Object.fromEntries(['BUY','SELL'].map(direction=>[direction,{trades:rows.filter(r=>r.direction===direction).length,byRatioBand:grid(rows.filter(r=>r.direction===direction)),cumulativeUpperThresholds:thresholdStudy(rows.filter(r=>r.direction===direction))}])),bySession:Object.fromEntries([...new Set(rows.map(r=>r.session).filter(Boolean))].map(session=>[session,{trades:rows.filter(r=>r.session===session).length,byRatioBand:grid(rows.filter(r=>r.session===session))}])),researchWarnings:['P-GAP candidate evidence is heuristic and is not yet a validated P-GAP definition.','Small bins can show unstable PF/AvgR and must not be treated as evidence of a robust edge.','The second-half split is an exploratory OOS check, not a formal walk-forward validation.','No threshold or filter is activated by this report.']};await mkdir(OUTPUT,{recursive:true});const out=resolve(OUTPUT,`${timeframe}.json`);await writeFile(out,JSON.stringify(outObj,null,2));console.log(`${timeframe}: trades=${rows.length} ratioP50=${outObj.fullSample.ratioDistribution.p50??'n/a'} ratioP90=${outObj.fullSample.ratioDistribution.p90??'n/a'}`);for(const[name,value]of Object.entries(outObj.fullSample.byRatioBand))console.log(`  ${name}: n=${value.trades} PF=${value.PF?.toFixed(4)??'n/a'} avgR=${value.avgR.toFixed(4)} totalR=${value.totalR.toFixed(4)} DD=${value.DD.toFixed(4)}`);console.log(`  OOS second-half: trades=${oos.length} PF=${outObj.oosSecondHalf.baseline.PF?.toFixed(4)??'n/a'} avgR=${outObj.oosSecondHalf.baseline.avgR.toFixed(4)}`);for(const[name,value]of Object.entries(outObj.oosSecondHalf.byRatioBand))console.log(`  OOS ${name}: n=${value.trades} PF=${value.PF?.toFixed(4)??'n/a'} avgR=${value.avgR.toFixed(4)} totalR=${value.totalR.toFixed(4)}`);console.log(`Report -> ${out}`)}
await analyze('1min');await analyze('5min');
