import{describe,expect,it}from"vitest";import{GEOMETRY_DEPENDENCIES}from"../research/fixtures/sp2l_geometry_dependency_matrix_v1.js";
describe("SP2L geometry dependency matrix",()=>{it("covers F08 F09 F10 F14",()=>expect(new Set(GEOMETRY_DEPENDENCIES.flatMap(x=>[x.from,x.to])).size).toBe(4));it("records dependency without promotion",()=>expect(GEOMETRY_DEPENDENCIES.every(x=>x.status==="UNRESOLVED"&&x.canonicalEligible===false)).toBe(true));it("does not infer AB=CD from stop or entry",()=>expect(GEOMETRY_DEPENDENCIES.filter(x=>x.to==="F14_ABCD").every(x=>x.note.includes("unresolved"))).toBe(true));});