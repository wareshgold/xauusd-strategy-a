import{describe,expect,it}from"vitest";import{BEARISH_GEOMETRY_MATRIX}from"../research/fixtures/sp2l_bearish_geometry_matrix_v1.js";
describe("SP2L F15 bearish geometry",()=>{it("covers all geometry stages",()=>expect(new Set(BEARISH_GEOMETRY_MATRIX.map(x=>x.stage)).size).toBe(4));it("keeps P-Gap evidence weaker than mirrored structural evidence",()=>{const p=BEARISH_GEOMETRY_MATRIX.find(x=>x.stage==="PGAP")!;expect(p.evidence).toBe("NOT_INDEPENDENTLY_DEMONSTRATED")});it("never promotes bearish geometry",()=>expect(BEARISH_GEOMETRY_MATRIX.every(x=>x.canonicalEligible===false)).toBe(true))});