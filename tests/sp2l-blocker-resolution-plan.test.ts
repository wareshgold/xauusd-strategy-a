import{describe,expect,it}from"vitest";import{BLOCKERS}from"../research/fixtures/sp2l_blocker_resolution_plan_v1.js";
describe("SP2L blocker resolution plan",()=>{it("contains all active blocker families",()=>expect(BLOCKERS.map(x=>x.family)).toEqual(expect.arrayContaining(["F08_SWING","F10_STOP","F09_ENTRY","F14_ABCD","F11_F12_EXECUTION","F13_2X","F15_BEARISH"])));it("never permits canonical promotion",()=>expect(BLOCKERS.every(x=>x.canonicalEligible===false)).toBe(true));it("keeps P0 before downstream work",()=>expect(BLOCKERS.filter(x=>x.priority==="P0").length).toBe(2));});