import{describe,expect,it}from"vitest";import{EXECUTION_MATRIX}from"../research/fixtures/sp2l_execution_semantics_matrix_v1.js";
describe("SP2L F11/F12 execution semantics",()=>{it("covers touch breach close and fill",()=>expect(new Set(EXECUTION_MATRIX.map(x=>x.event)).size).toBe(4));it("covers lifecycle transitions",()=>expect(new Set(EXECUTION_MATRIX.map(x=>x.lifecycle)).size).toBeGreaterThanOrEqual(3));it("covers both directions",()=>expect(new Set(EXECUTION_MATRIX.map(x=>x.direction)).size).toBe(2));it("never promotes semantics",()=>expect(EXECUTION_MATRIX.every(x=>x.status==="SOURCE_SHAPED_UNRESOLVED"&&x.canonicalEligible===false)).toBe(true));});