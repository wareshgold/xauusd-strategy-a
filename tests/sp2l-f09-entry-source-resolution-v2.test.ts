import{describe,expect,it}from"vitest";import{F09_CASES}from"../research/fixtures/sp2l_f09_entry_source_resolution_v2.js";
describe("F09 entry source resolution v2",()=>{it("covers both directions",()=>expect(new Set(F09_CASES.map(x=>x.direction)).size).toBe(2));it("covers one two three candle family",()=>expect(new Set(F09_CASES.map(x=>x.triggerCandleCount)).size).toBe(3));it("covers entry hypotheses",()=>expect(new Set(F09_CASES.map(x=>x.entry)).size).toBe(4));it("fails closed",()=>expect(F09_CASES.every(x=>x.status==="SOURCE_SUPPORTED_FIELD_UNRESOLVED"&&x.canonicalEligible===false)).toBe(true));});