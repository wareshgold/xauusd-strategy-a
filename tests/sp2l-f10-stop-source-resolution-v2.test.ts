import{describe,expect,it}from"vitest";import{F10_CASES}from"../research/fixtures/sp2l_f10_stop_source_resolution_v2.js";
describe("F10 stop source resolution v2",()=>{it("covers both directions",()=>expect(new Set(F10_CASES.map(x=>x.direction)).size).toBe(2));it("covers wick/body/structural/buffer hypotheses",()=>expect(new Set(F10_CASES.map(x=>x.anchor)).size).toBe(4));it("covers invalidation events",()=>expect(new Set(F10_CASES.map(x=>x.event)).size).toBe(3));it("fails closed",()=>expect(F10_CASES.every(x=>x.status==="SOURCE_SUPPORTED_FIELD_UNRESOLVED"&&x.canonicalEligible===false)).toBe(true));});