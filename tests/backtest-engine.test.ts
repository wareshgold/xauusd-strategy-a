import { describe, expect, it } from 'vitest';
import type { Candle } from '../src/domain/market/Candle.js';
import { runBacktest } from '../src/backtest/BacktestEngine.js';
const c=(i:number,o:number,h:number,l:number,cl:number):Candle=>({timestamp:`2026-01-01T00:${String(i).padStart(2,'0')}:00Z`,open:o,high:h,low:l,close:cl});
describe('backtest engine',()=>{it('records TP1 and SL deterministically',()=>{const candles=[c(0,100,101,99,100),c(1,100,103,100,102),c(2,102,106,102,105),c(3,101,103,100,102),c(4,102,106,101,105),c(5,105,106,104,105)];const result=runBacktest(candles,[{entryIndex:3,entryTime:candles[3]!.timestamp,direction:'BUY',entry:102,stopLoss:100,tp1:105},{entryIndex:1,entryTime:candles[1]!.timestamp,direction:'BUY',entry:102,stopLoss:99,tp1:104}]);expect(result.metrics.trades).toBe(2);expect(result.metrics.wins).toBe(2);expect(result.metrics.winRate).toBe(1);});it('marks same-candle TP and SL as ambiguous',()=>{const candles=[c(0,100,101,99,100),c(1,100,105,95,100)];const result=runBacktest(candles,[{entryIndex:0,entryTime:candles[0]!.timestamp,direction:'BUY',entry:100,stopLoss:97,tp1:103}]);expect(result.trades[0]!.result).toBe('AMBIGUOUS');});});
