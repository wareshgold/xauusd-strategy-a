import {describe,expect,it} from "vitest"; import {ABCD_ANCHOR_MATRIX} from "../research/fixtures/sp2l_abcd_anchor_matrix_v1.js";
describe("SP2L AB=CD anchor matrix",()=>{it("keeps competing anchor families distinct",()=>expect(new Set(ABCD_ANCHOR_MATRIX.map(x=>x.family)).size).toBe(3));it("covers bullish and bearish mirrors",()=>{expect(ABCD_ANCHOR_MATRIX.some(x=>x.direction==="BUY")).toBe(true);expect(ABCD_ANCHOR_MATRIX.some(x=>x.direction==="SELL")).toBe(true)});it("does not invent a tolerance",()=>expect(ABCD_ANCHOR_MATRIX.every(x=>x.toleranceStatus==="UNRESOLVED"&&x.canonicalEligible===false)).toBe(true));it("contains exact, near-equal and materially unequal cases",()=>{expect(ABCD_ANCHOR_MATRIX.some(x=>x.ratio===1)).toBe(true);expect(ABCD_ANCHOR_MATRIX.some(x=>x.ratio===1.05)).toBe(true);expect(ABCD_ANCHOR_MATRIX.some(x=>x.ratio===1.25)).toBe(true)})});