import{describe,expect,it}from"vitest";import{LIFECYCLE_CASES}from"../research/fixtures/sp2l_pending_order_lifecycle_v1.js";
describe("SP2L pending-order lifecycle",()=>{it("keeps replacement and cancellation reasons distinct",()=>expect(new Set(LIFECYCLE_CASES.map(x=>x.reason)).size).toBe(4));it("keeps timeout and fill unresolved",()=>{expect(LIFECYCLE_CASES.find(x=>x.id==="LIFE-003")?.reason).toBe("UNRESOLVED_TIMEOUT");expect(LIFECYCLE_CASES.find(x=>x.id==="LIFE-004")?.reason).toBe("UNRESOLVED_FILL")});it("never promotes lifecycle",()=>expect(LIFECYCLE_CASES.every(x=>x.canonicalEligible===false)).toBe(true));});