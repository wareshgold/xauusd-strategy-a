import{describe,expect,it}from"vitest";import{MATRIX}from"../research/fixtures/sp2l_swing_entry_geometry_matrix_v1.js";
describe("SP2L F08/F09 geometry matrix",()=>{it("covers swing and entry hypotheses",()=>{expect(MATRIX.some(x=>x.family==="F08_SWING")).toBe(true);expect(MATRIX.some(x=>x.family==="F09_ENTRY")).toBe(true)});it("covers both directions",()=>expect(new Set(MATRIX.map(x=>x.direction)).size).toBe(2));it("never promotes unresolved hypotheses",()=>expect(MATRIX.every(x=>x.sourceStatus==="SOURCE_SHAPED_UNRESOLVED"&&x.canonicalEligible===false)).toBe(true));it("keeps close-reclaim separate from pending-limit hypotheses",()=>expect(new Set(MATRIX.filter(x=>x.family==="F09_ENTRY").map(x=>x.hypothesis)).size).toBe(3))});