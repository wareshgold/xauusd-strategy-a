import{describe,expect,it}from"vitest";import{RESOLUTION_PRIORITY}from"../research/fixtures/sp2l_resolution_priority_matrix_v1.js";
describe("SP2L resolution priority",()=>{it("identifies P0 source blockers",()=>expect(RESOLUTION_PRIORITY.filter(x=>x.priority==="P0_SOURCE_BLOCKER").map(x=>x.family)).toEqual(expect.arrayContaining(["F08_SWING","F10_STOP"])));it("keeps all items noncanonical",()=>expect(RESOLUTION_PRIORITY.every(x=>x.canonicalEligible===false)).toBe(true));it("does not claim geometry is frozen",()=>expect(RESOLUTION_PRIORITY.some(x=>x.family==="F14_ABCD")).toBe(true));});